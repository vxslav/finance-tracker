export default class Expense {
    constructor(id, desc, amount, date, category) {
        this.id = id,
        this.desc = desc,
        this.amount = amount,
        this.date = date,
        this.category = category;
    }
}